[package]
name = "envstow"
version = "0.1.16"
edition = "2021"
description = "An age-encrypted key-value secret store, committed to your repo and surfaced by name."
license = "MIT"
repository = "https://github.com/jhnhnsn/envstow"

[[bin]]
name = "envstow"
path = "src/main.rs"

[dependencies]
zeroize = "1"
age = "0.11"
base64 = "0.21"

[profile.release]
strip = true
lto = true
opt-level = "z"
